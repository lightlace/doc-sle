<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xml:id="cha.yast.text">
<!-- ================================================================== -->
 <title>&yast; in Text Mode</title>
 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker></dm:bugtracker>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <para>
  This section is intended for system administrators and experts who do not run
  an X server on their systems and depend on the text-based installation tool.
  It provides basic information about starting and operating &yast; in text
  mode.
 </para>
 <para>
  &yast; in text mode uses the ncurses library to provide an easy
  pseudo-graphical user interface. The ncurses library is installed by default.
  The minimum supported size of the terminal emulator in which to run &yast; is
  80x25 characters.
 </para>
 <figure xml:id="fig.yast2.ncurses">
  <title>Main Window of &yast; in Text Mode</title>
  <mediaobject>
   <imageobject role="fo">
    <imagedata fileref="yast2_ncurses_main.png" width="70%" format="PNG"/>
   </imageobject>
   <imageobject role="html">
    <imagedata fileref="yast2_ncurses_main.png" width="75%" format="PNG"/>
   </imageobject>
  </mediaobject>
 </figure>
 <para>
  When you start &yast; in text mode, the &yast; control center appears (see
  <xref linkend="fig.yast2.ncurses" xrefstyle="select:label nopage"/>). The
  main window consists of three areas. The left frame features the categories
  to which the various modules belong. This frame is active when &yast; is
  started and therefore it is marked by a bold white border. The active
  category is selected. The right frame provides an overview of the modules
  available in the active category. The bottom frame contains the buttons for
  <guimenu>Help</guimenu> and <guimenu>Quit</guimenu>.
 </para>
 <para>
  When you start the &yast; control center, the category
  <guimenu>Software</guimenu> is selected automatically. Use
  <keycap function="down"/> and <keycap function="up"/> to change the category.
  To select a module from the category, activate the right frame with
  <keycap function="right"/> and then use <keycap function="down"/> and
  <keycap function="up"/> to select the module. Keep the arrow keys pressed to
  scroll through the list of available modules. The selected module is
  selected. Press <keycap function="enter"/> to start the active module.
 </para>
 <para>
  Various buttons or selection fields in the module contain a highlighted
  letter (yellow by default). Use
  <keycombo action="press"><keycap function="alt"/><keycap>highlighted_letter</keycap></keycombo>
  to select a button directly instead of navigating there with
  <keycap function="tab"/>. Exit the &yast; control center by pressing
  <keycombo action="press"><keycap function="alt"/><keycap>Q</keycap></keycombo>
  or by selecting <guimenu>Quit</guimenu> and pressing
  <keycap function="enter"/>.
 </para>
 <tip>
<!-- bnc#734717 -->
  <title>Refreshing &yast; Dialogs</title>
  <para>
   If a &yast; dialog gets corrupted or distorted (for example, while resizing
   the window), press
   <keycombo action="press"><keycap function="control"/><keycap>L</keycap></keycombo>
   to refresh and restore its contents.
  </para>
 </tip>
<!-- ================================================================== -->
 <sect1 xml:id="sec.yast.cli.navigate">
  <title>Navigation in Modules</title>

  <para>
   The following description of the control elements in the &yast; modules
   assumes that all function keys and <keycap function="alt"/> key combinations
   work and are not assigned to different global functions. Read
   <xref linkend="sec.yast.cli.restricted_keys"/> for information about
   possible exceptions.
  </para>

  <variablelist>
   <varlistentry>
    <term>Navigation among Buttons and Selection Lists</term>
    <listitem>
     <para>
      Use <keycap function="tab"/> to navigate among the buttons and frames
      containing selection lists. To navigate in reverse order, use
      <keycombo action="press"> <keycap function="alt"/>
      <keycap function="tab"/> </keycombo> or <keycombo action="press">
      <keycap function="shift"/> <keycap function="tab"/> </keycombo>
      combinations.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>Navigation in Selection Lists</term>
    <listitem>
     <para>
      Use the arrow keys (<keycap function="up"/> and
      <keycap function="down"/>) to navigate among the individual elements in
      an active frame containing a selection list. If individual entries within
      a frame exceed its width, use
      <keycombo action="press"><keycap function="shift"/>
      <keycap function="right"/></keycombo> or <keycombo action="press">
      <keycap function="shift"/> <keycap function="left"/></keycombo> to scroll
      horizontally to the right and left. Alternatively, use
      <keycombo action="press"> <keycap function="control"/> <keycap>E</keycap>
      </keycombo> or <keycombo action="press"> <keycap function="control"/>
      <keycap>A</keycap> </keycombo>. This combination can also be used if
      using <keycap function="right"/> or <keycap function="left"/> results in
      changing the active frame or the current selection list, as in the
      control center.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>Buttons, Radio Buttons, and Check Boxes</term>
    <listitem>
     <para>
      To select buttons with empty square brackets (check boxes) or empty
      parentheses (radio buttons), press <keycap function="space"/> or
      <keycap function="enter"/>. Alternatively, radio buttons and check boxes
      can be selected directly with <keycombo action="press">
      <keycap function="alt"/> <keycap>highlighted_letter</keycap></keycombo>.
      In this case, you do not need to confirm with <keycap function="enter"/>.
      If you navigate to an item with <keycap function="tab"/>, press
      <keycap function="enter"/> to execute the selected action or activate the
      respective menu item.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>Function Keys</term>
    <listitem>
     <para>
      The function keys (<keycap>F1</keycap> ... <keycap>F12</keycap>) enable
      quick access to the various buttons. Available function key combinations
      (<literal>F<replaceable>X</replaceable></literal>) are shown in the
      bottom line of the &yast; screen. Which function keys are actually mapped
      to which buttons depend on the active &yast; module, because the
      different modules offer different buttons (<guimenu>Details</guimenu>,
      <guimenu>Info</guimenu>, <guimenu>Add</guimenu>,
      <guimenu>Delete</guimenu>, etc.). Use <keycap>F10</keycap> for
      <guimenu>Accept</guimenu>, <guimenu>OK</guimenu>,
      <guimenu>Next</guimenu>, and <guimenu>Finish</guimenu>. Press
      <keycap>F1</keycap> to access the &yast; help.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>Using Navigation Tree in ncurses Mode</term>
    <listitem>
     <para>
      Some &yast; modules use a navigation tree in the left part of the window
      to select configuration dialogs. Use the arrow keys
      (<keycap function="up"/> and <keycap function="down"/>) to navigate in
      the tree. Use <keycap function="space"/> to open or close tree items. In
      ncurses mode, <keycap function="enter"/> must be pressed after a
      selection in the navigation tree to show the selected dialog. This is an
      intentional behavior to save time consuming redraws when browsing through
      the navigation tree.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>Selecting Software in the Software Installation Module</term>
    <listitem>
     <para>
      Use the filters on the left side to limit the amount of displayed
      packages. Installed packages are marked with the letter
      <literal>i</literal>. To change the status of a package, press
      <keycap function="space"/> or <keycap function="enter"/>. Alternatively,
      use the <guimenu>Actions</guimenu> menu to select the needed status
      change (install, delete, update, taboo or lock).
     </para>
    </listitem>
   </varlistentry>
  </variablelist>

  <figure xml:id="fig.yast2.ncurses_inst">
   <title>The Software Installation Module</title>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="yast2_ncurses_inst.png" width="70%" format="PNG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="yast2_ncurses_inst.png" width="75%" format="PNG"/>
    </imageobject>
   </mediaobject>
  </figure>
 </sect1>
 <sect1 xml:id="sec.yast.cli.key_combination">
  <title>Advanced Key Combinations</title>

  <para>
   &yast; in text mode has a set of advanced key combinations.
  </para>

  <variablelist>
   <varlistentry>
    <term><keycombo action="press"><keycap function="shift"/><keycap>F1</keycap></keycombo>
    </term>
    <listitem>
     <para>
      Show a list of advanced hotkeys.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><keycombo action="press"><keycap function="shift"/><keycap>F4</keycap></keycombo>
    </term>
    <listitem>
     <para>
      Change color schema.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><keycombo action="press"><keycap function="control"/><keycap>\</keycap></keycombo>
    </term>
    <listitem>
     <para>
      Quit the application.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><keycombo action="press"><keycap function="control"/><keycap>L</keycap></keycombo>
    </term>
    <listitem>
     <para>
      Refresh screen.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><keycombo action="press"><keycap function="control"/><keycap>D</keycap></keycombo><keycap>F1</keycap>
    </term>
    <listitem>
     <para>
      Show a list of advanced hotkeys.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><keycombo action="press"><keycap function="control"/><keycap>D</keycap></keycombo><keycombo action="press"><keycap function="shift"/><keycap>D</keycap></keycombo>
    </term>
    <listitem>
     <para>
      Dump dialog to the log file as a screenshot.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><keycombo action="press"><keycap function="control"/><keycap>D</keycap></keycombo><keycombo action="press"><keycap function="shift"/><keycap>Y</keycap></keycombo>
    </term>
    <listitem>
     <para>
      Open YDialogSpy to see the widget hierarchy.
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect1>
 <sect1 xml:id="sec.yast.cli.restricted_keys">
  <title>Restriction of Key Combinations</title>

  <para>
   If your window manager uses global <keycap function="alt"/> combinations,
   the <keycap function="alt"/> combinations in &yast; might not work. Keys
   like <keycap function="alt"/> or <keycap function="shift"/> can also be
   occupied by the settings of the terminal.
  </para>

  <variablelist>
   <varlistentry>
    <term>Replacing <keycap function="alt"/> with <keycap function="escape"/>
    </term>
    <listitem>
     <para>
      <keycap function="alt"/> shortcuts can be executed with
      <keycap function="escape"/> instead of <keycap function="alt"/>. For
      example, <keycombo action="seq">
      <keycap function="escape"/><keycap>H</keycap></keycombo> replaces
      <keycombo action="press">
      <keycap function="alt"/><keycap>H</keycap></keycombo>. (First press
      <keycap function="escape"/>, <emphasis>then</emphasis> press
      <keycap>H</keycap>.)
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term> Backward and Forward Navigation with <keycombo action="press"><keycap function="control"/><keycap>F</keycap></keycombo> and <keycombo action="press"><keycap function="control"/><keycap>B</keycap></keycombo>
    </term>
    <listitem>
     <para>
      If the <keycap function="alt"/> and <keycap function="shift"/>
      combinations are occupied by the window manager or the terminal, use the
      combinations <keycombo action="press"> <keycap function="control"/>
      <keycap>F</keycap></keycombo> (forward) and <keycombo action="press">
      <keycap function="control"/><keycap>B</keycap></keycombo> (backward)
      instead.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>Restriction of Function Keys</term>
    <listitem>
     <para>
      The function keys (<keycap>F1</keycap> ... <keycap>F12</keycap>) are also
      used for functions. Certain function keys might be occupied by the
      terminal and may not be available for &yast;. However, the
      <keycap function="alt"/> key combinations and function keys should always
      be fully available on a pure text console.
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect1>
 <sect1 xml:id="sec.yast.cli.options">
  <title>&yast; Command Line Options</title>

  <para>
   Besides the text mode interface, &yast; provides a pure command line
   interface. To get a list of &yast; command line options, enter:
  </para>

<screen>&prompt.sudo;yast -h</screen>

  <sect2 xml:id="sec.yast.cli.options.install">
   <title>Installing Packages from the Command Line</title>
   <para>
    If you know a package name and the package is provided by any of your
    active installation repositories, you can use the command line option
    <option>-i</option> to install the package:
   </para>
<screen>&prompt.sudo;yast <replaceable>-i package_name</replaceable></screen>
   <para>
    or
   </para>
<screen>&prompt.sudo;yast --install <replaceable>-i package_name</replaceable></screen>
   <para>
    <replaceable>package_name</replaceable> can be a single short package name
    (for example <package>gvim</package>) installed with dependency checking,
    or the full path to an RPM package which is installed without dependency
    checking.
   </para>
   <para>
    If you need a command line based software management utility with
    functionality beyond what &yast; provides, consider using Zypper. This
    utility uses the same software management library that is also the
    foundation for the &yast; package manager. The basic usage of Zypper is
    covered in <xref linkend="sec.zypper"/>.
   </para>
  </sect2>

  <sect2 xml:id="sec.yast.cli.options.modules">
   <title>Starting the Individual Modules</title>
   <para>
    To save time, the individual &yast; modules can be started directly. To
    start a module, enter:
   </para>
<screen>&prompt.sudo;yast <replaceable>module_name</replaceable></screen>
   <para>
    View a list of all module names available on your system with <command>yast
    -l</command> or <command>yast --list</command>. Start the network module,
    for example, with <command>yast lan</command>.
   </para>
  </sect2>

  <sect2 xml:id="sec.yast.cli.options.module_params">
   <title>Command Line Parameters of the &yast; Modules</title>
   <para>
    To use &yast; functionality in scripts, &yast; provides command line
    support for individual modules. Not all modules have command line support.
    To display the available options of a module, enter:
   </para>
<screen>&prompt.sudo;yast <replaceable>module_name</replaceable> help</screen>
   <para>
    If a module does not provide command line support, it is started in a text
    mode and the following message appears:
   </para>
<screen>This &yast; module does not support the command line interface.</screen>
   <para>
    The following sections describe all &yast; modules with command line
    support, together with a brief explanation of all their commands and
    available options.
   </para>
   <sect3>
    <title>yast add-on</title>
    <para>
     Adds a new Add-On product from the specified path:
    </para>
<screen>
 &prompt.sudo;yast add-on http://server.name/directory/Lang-AddOn-CD1/
</screen>
    <para>
     You can use the following protocols to specify the source path: http://
     ftp:// nfs:// disk:// cd:// or dvd://.
    </para>
   </sect3>
   <sect3>
    <title>yast audit-laf</title>
    <para>
     Displays and configures the Linux Audit Framework. Refer to
     <xref
      linkend="part.audit"/> for more details. <command>yast
     audit-laf</command> accepts the following commands:
    </para>
    <variablelist>
     <varlistentry>
      <term>set</term>
      <listitem>
       <para>
        Sets the specified option:
       </para>
<screen>
&prompt.sudo;yast audit-laf set log_file=/tmp/audit.log
</screen>
       <para>
        For a complete list of options, run <command>yast audit-laf set
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>show</term>
      <listitem>
       <para>
        Displays settings of the specified option:
       </para>
<screen>
&prompt.sudo;yast audit-laf show diskspace
space_left: 75
space_left_action: SYSLOG
admin_space_left: 50
admin_space_left_action: SUSPEND
action_mail_acct: root
disk_full_action: SUSPEND
disk_error_action: SUSPEND
</screen>
       <para>
        For a complete list of options, run <command>yast audit-laf show
        help</command>.
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </sect3>
   <sect3>
    <title>yast dhcp-server</title>
    <para>
     Manages the DHCP server and configures its settings. <command>yast
     dhcp-server</command> accepts the following commands:
    </para>
    <variablelist>
     <varlistentry>
      <term>disable</term>
      <listitem>
       <para>
        Disables the DHCP server service.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>enable</term>
      <listitem>
       <para>
        Enables the DHCP server service.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>host</term>
      <listitem>
       <para>
        Configure settings for individual hosts.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>interface</term>
      <listitem>
       <para>
        Specifies to which network interface to listen to:
       </para>
<screen>
&prompt.sudo;yast dhcp-server interface current
Selected Interfaces: eth0
Other Interfaces: bond0, pbu, eth1
</screen>
       <para>
        For a complete list of options, run <command>yast dhcp-server interface
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>options</term>
      <listitem>
       <para>
        Manages global DHCP options. For a complete list of options, run
        <command>yast dhcp-server options help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>status</term>
      <listitem>
       <para>
        Prints the status of the DHCP service.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>subnet</term>
      <listitem>
       <para>
        Manages the DHCP subnet options. For a complete list of options, run
        <command>yast dhcp-server subnet help</command>
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </sect3>
   <sect3>
    <title>yast dns-server</title>
    <para>
     Manages the DNS server configuration. <command>yast dns-server</command>
     accepts the following commands:
    </para>
    <variablelist>
     <varlistentry>
      <term>acls</term>
      <listitem>
       <para>
        Displays access control list settings:
       </para>
<screen>
 &prompt.sudo;yast dns-server acls show
 ACLs:
 -----
  Name       Type        Value
  ----------------------------
  any        Predefined
  localips   Predefined
  localnets  Predefined
  none       Predefined
</screen>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>dnsrecord</term>
      <listitem>
       <para>
        Configures zone resource records:
       </para>
<screen>
&prompt.sudo;yast dnsrecord add zone=example.org query=office.example.org type=NS value=ns3
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server dnsrecord
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>forwarders</term>
      <listitem>
       <para>
        Configures DNS forwarders:
       </para>
<screen>
&prompt.sudo;yast dns-server forwarders add ip=10.0.0.100
&prompt.sudo;yast dns-server forwarders show
[...]
Forwarder IP
------------
10.0.0.100
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server forwarders
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>host</term>
      <listitem>
       <para>
        Handles 'A' and its related 'PTR' record at once:
       </para>
<screen>
&prompt.sudo;yast dns-server host show zone=example.org
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server host
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>logging</term>
      <listitem>
       <para>
        Configures logging settings:
       </para>
<screen>
&prompt.sudo;yast dns-server logging set updates=no transfers=yes
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server logging
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>mailserver</term>
      <listitem>
       <para>
        Configures zone mail servers:
       </para>
<screen>
&prompt.sudo;yast dns-server mailserver add zone=example.org mx=mx1 priority=100
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server mailserver
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>nameserver</term>
      <listitem>
       <para>
        Configures zone name servers:
       </para>
<screen>
&prompt.sudo;yast dns-server nameserver add zone=example.com ns=ns1
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server nameserver
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>soa</term>
      <listitem>
       <para>
        Configures the start of authority (SOA) record:
       </para>
<screen>
&prompt.sudo;yast dns-server soa set zone=example.org serial=2006081623 ttl=2D3H20S
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server soa
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>startup</term>
      <listitem>
       <para>
        Manages the DNS server service:
       </para>
<screen>
&prompt.sudo;yast dns-server startup atboot
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server startup
        help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>transport</term>
      <listitem>
       <para>
        Configure zone transport rules. For a complete list of options, run
        <command>yast dns-server transport help</command>.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>zones</term>
      <listitem>
       <para>
        Manages DNS zones:
       </para>
<screen>
&prompt.sudo;yast dns-server zones add name=example.org zonetype=master
</screen>
       <para>
        For a complete list of options, run <command>yast dns-server zones
        help</command>.
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </sect3>
   <sect3>
    <title>yast disk</title>
    <para>
     Prints information about all disks or partitions. The only supported
     command is <command>list</command> followed by any of the following
     options:
    </para>
    <variablelist>
     <varlistentry>
      <term>disks</term>
      <listitem>
       <para>
        Lists all configured disks in the system:
       </para>
<screen>
&prompt.sudo;yast disk list disks
Device   | Size       | FS Type | Mount Point | Label | Model
---------+------------+---------+-------------+-------+-------------
/dev/sda | 119.24 GiB |         |             |       | SSD 840
/dev/sdb |  60.84 GiB |         |             |       | WD1003FBYX-0
</screen>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>partitions</term>
      <listitem>
       <para>
        Lists all partitions in the system:
       </para>
<screen>
&prompt.sudo;yast disk list partitions
Device         | Size       | FS Type | Mount Point | Label | Model
---------------+------------+---------+-------------+-------+------
/dev/sda1      |   1.00 GiB | Ext2    | /boot       |       |
/dev/sdb1      |   1.00 GiB | Swap    | swap        |       |
/dev/sdc1      | 698.64 GiB | XFS     | /mnt/extra  |       |
/dev/vg00/home | 580.50 GiB | Ext3    | /home       |       |
/dev/vg00/root | 100.00 GiB | Ext3    | /           |       |
[...]
</screen>
      </listitem>
     </varlistentry>
    </variablelist>
   </sect3>
   <sect3>
    <title>yast firewall</title>
    <para>
     Displays information about the firewall settings. <command>yast
     firewall</command> accepts the following commands:
    </para>
    <variablelist>
     <varlistentry>
      <term>broadcast</term>
      <listitem>
       <para>
        Displays settings of broadcast packets.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>disable</term>
      <listitem>
       <para>
        Disables firewall.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>enable</term>
      <listitem>
       <para>
        Enables firewall.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>interfaces</term>
      <listitem>
       <para>
        Displays the configuration related to network interfaces.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>logging</term>
      <listitem>
       <para>
        Displays the logging settings.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>masqredirect</term>
      <listitem>
       <para>
        Redirects requests to masqueraded IP.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>masquerade</term>
      <listitem>
       <para>
        Displays the masquerading settings.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>services</term>
      <listitem>
       <para>
        Displays information about allowed services, ports, and protocols.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>startup</term>
      <listitem>
       <para>
        Displays startup settings.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>summary</term>
      <listitem>
       <para>
        Displays firewall configuration summary.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>zones</term>
      <listitem>
       <para>
        Lists known firewall zones.
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
    <tip>
     <title>Interactive Mode</title>
     <para>
      If you need to spend more time querying the firewall settings, run the
      <emphasis>interactive</emphasis> mode.
     </para>
<screen>
&prompt.sudo;yast firewall interactive
YaST2 firewall> enable
</screen>
     <para>
      The <literal>YaST2 firewall</literal> shell opens, where you can enter
      all the <command>yast firewall</command> commands without the
      <command>sudo yast firewall</command> prefix. To leave the interactive
      mode, enter <literal>exit</literal>.
     </para>
    </tip>
   </sect3>
   <sect3>
    <title>yast ftp-server</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast http-server</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast kdump</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast keyboard</title>
    <para>
     Configures the system keyboard for virtual consoles. It does not affect
     the keyboard settings in graphical desktop environments, such as &gnome;
     or KDE. <command>yast keyboard</command> accepts the following commands:
    </para>
    <variablelist>
     <varlistentry>
      <term>list</term>
      <listitem>
       <para>
        Lists all available keyboard layouts.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>set</term>
      <listitem>
       <para>
        Activates new keyboard layout setting:
       </para>
<screen>
&prompt.sudo;yast keyboard set layout=czech
</screen>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>summary</term>
      <listitem>
       <para>
        Displays the current keyboard configuration.
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </sect3>
   <sect3>
    <title>yast lan</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast language</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast mail</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast nfs</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast nfs-server</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast nis</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast nis-server</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast proxy</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast rdp</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast rear</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast samba-client</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast samba-server</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast security</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast sound</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast sysconfig</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast tftp-server</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast timezone</title>
    <para></para>
   </sect3>
   <sect3>
    <title>yast users</title>
    <para></para>
   </sect3>
  </sect2>
 </sect1>
</chapter>
